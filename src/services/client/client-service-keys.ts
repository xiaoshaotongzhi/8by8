/**
 * An object containing keys which can be used to register services to and
 * retrieve services from the `clientServicesContainer`.
 */
export const CLIENT_SERVICE_KEYS = {
  UserService: Symbol.for('UserService'),
};
